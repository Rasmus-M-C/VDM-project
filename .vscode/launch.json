{
    "configurations": [
        {
            "name": "Launch ParkGarage Debug",
            "type": "vdm",
            "request": "launch",
            "noDebug": false,
            "defaultName": "World",
            "command": "print new world().Run()"
        }
    ]
}